#include<stdio.h>
#include "main.h"

/**
 *mul	-	a function that multiplies two integers;
 *
 *@a:	takes an int and multiple with @b;
 *@b:	takes an input and multiply with @a;
 *Return: return total_mul;
 *
 */

int mul(int a, int b)
{
	int total_mul;

	total_mul = (a * b);
	return (total_mul);
}

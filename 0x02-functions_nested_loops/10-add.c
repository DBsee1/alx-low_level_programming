#include "main.c"

/**
 *add - unction that adds two integers and returns the result;
 *
 * @first_agr:	taking input (int);
 * @second_agr:	taking the second input(int);
 *
 *Return: Return Add_sum;
 */

int add(int first_agr, int second_agr)
{
	int total_sum;

	total_sum = (first_agr + second_agr);
	return (total_sum);
}

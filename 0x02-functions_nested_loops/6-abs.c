#include "main.h"

/**
 *_abs	-	 a function that computes the absolute value of an integer;
 *@int:	a data type;
 * Return:	Always return (0);
 */

int _abs(int)
{
	if (int < 0)
		return (-int);
	else
		return (int);
}
